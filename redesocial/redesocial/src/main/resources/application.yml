spring:
  application:
    name: redesocial

  datasource:
    url: "jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require"
    username: postgres.afdgwnyozvufhzdtuttt
    password: SenhaSuperForte
    driver-class-name: org.postgresql.Driver
    hikari:
      # Keep the pool small to avoid exhausting the Supabase/pgbouncer client limit
      maximum-pool-size: 5
      minimum-idle: 1
      max-lifetime: 600000
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  public:
    key: classpath:authz.pub
  private:
    key: classpath:authz.pem
